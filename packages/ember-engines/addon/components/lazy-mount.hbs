{{this.load @name}}

{{#if this.isLoading}}
  {{yield to="loading"}}
{{else if this.error}}
  {{yield this.error to="error"}}
{{else if this.loadedName}}
  {{mount this.loadedName model=@model}}
{{/if}}